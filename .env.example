# Zoho Mail SMTP — used by /api/contact on Vercel
# Create an app-specific password: Zoho Mail → Settings → Security → App Passwords
ZOHO_SMTP_USER=
ZOHO_SMTP_PASS=
ZOHO_SMTP_HOST=
ZOHO_SMTP_PORT=

# Inbox that receives contact form submissions (defaults to ZOHO_SMTP_USER)
CONTACT_TO_EMAIL=
